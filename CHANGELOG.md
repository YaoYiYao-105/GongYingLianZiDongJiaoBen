# 更新日志

本文件记录本项目的所有重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

## [0.2.0] - 2026-09-18

### 新增

- **接口方式的执行链路**（`src/api.py`、`src/api_workflow.py`），并成为默认方式。
  复刻后台网页自己发出的请求：同样的地址、请求体与签名算法，因此不再需要逐个单元格点击。
  实测读取一个订单的 120 行商品耗时约 16 秒，而点击方式是分钟级。
- `src/api.py`：签名算法（`param_tamp` / `data_tamp` / `timestamp_tamp`）、凭据解析、
  箱码计划生成，以及把保存结果**重新读回来逐行核对**的校验函数。
- `src/api_workflow.py`：接口方式的编排。会话缓存在用户目录的 `config\session.json`（权限 600），
  第一次登录后日常运行不再打开任何浏览器窗口；缓存被服务端拒绝时才回退到浏览器读取一次。
- 命令行新增 `--mode {api,browser}`；`main.py login` 登录成功后立即缓存会话。
- 配置项 `behavior.box_code`：要写入的箱码，默认为 `1`。

### 变更

- `src/workflow.py` 拆成两个驱动：`run()` 按 `mode` 分发，原来的点击流程改名 `run_browser()`。
  默认方式改为 `api`，点击方式保留作为前端改版时的兜底。
- 接口方式**不做日期过滤**。订货通知单的开单日期是到货前一天，
  按「今日的订单」去查会静默返回空结果，而订单「仍未执行」才是真正代表有活要干的状态。

### 修复

- 试运行也会写进断点续跑的日志，导致随后真正的 `--commit` 运行把这些订单全部跳过 —— 
  等于什么都没做却报告成功。现在试运行不再留下任何日志记录。

### 测试

- 用例总数 57 → 115。新增 `tests/test_api.py`（32）与 `tests/test_api_workflow.py`（24），
  两者都不需要浏览器。签名算法用固定的字节序列钉死，改动一个字符即失败。
- 写入校验的负向用例：服务端回 `200000` 但其实没落库时，必须判为失败而不是成功。

## [0.1.3] - 2026-09-17

### 变更

- 运行期间不再持续刷新界面日志，改为结束（或中途停止）时只弹出一次结果对话框。
- 弹窗期间窗口临时置顶一次，关闭后立即取消置顶 —— 埋在最大化窗口后面的通知不叫通知。
- 窗口标题会变为「完成」或「有失败项」，方便从任务栏一眼确认结果。

## [0.1.2] - 2026-09-17

### 新增

- `src/errors.py`：把底层异常翻译成操作者看得懂、能照做的一句话，覆盖网络错误、
  未登录、手动中断、定位失败等场景。开发者向的堆栈仍然保留在日志里。
- `RunReport.exit_code`：用退出码反映结果（成功 / 有失败项 / 中止），便于脚本化调用。

### 修复

- 运行中止时明确输出 `ABORTED — <原因>`，而不是打印一份看起来像「今天没有订单」的空统计。
  后者会让人误以为已经跑完了，这是这个工具最危险的一种失败方式。
- 原始异常堆栈改为写入 `runs/<时间戳>/run.log`，不再刷到控制台。

### 测试

- 补充异常翻译、界面状态流转与中止路径的用例。

## [0.1.1] - 2026-09-17

### 新增

- `demo/mock_portal.py` 与 `scripts/demo_local.py`：一个复现四个页面行为的本地模拟后台，
  可以在没有供应商账号的情况下完整走通链路，并直接看到模拟后台实际收到了什么。
- `tests/test_live_portal.py`：真实 Chromium 驱动真实 HTTP 页面，断言的是服务端实际收到的数据，
  而不是代码以为它干了什么。
- `SUPPLIER_AUTOMATION_HOME` 环境变量，用于重定向数据目录，使演示与测试不会碰到真实登录态。

### 修复

- `collect_order_numbers` 因 `resolve()` 收窄为单个元素，**只会采集到第一个订单号**，其余全部漏掉。
- 处理完一个订单返回列表页后没有重新执行查询，导致从第二个订单起找不到入口。
- 默认配置里使用了 `role=link[name*=...]` 和 `text=/正则/` 两种解析器并不支持的写法。
  角色参数现在会在配置加载时校验，写错立即报错，而不是等到点击时才炸。

### 测试

- 用例总数 20 → 30，其中 3 个使用真实浏览器。

## [0.1.0] - 2026-09-17

### 新增

- 首次公开发布。
- 分层结构：业务模块只描述「做什么」，全部元素定位集中在 `src/config.py`，前端改版只需改一个文件。
- 默认试运行，必须显式传 `--commit` 才会写入生产后台。
- 幂等填充、部分失败不提交、JSONL 断点续跑。
- 持久化浏览器 profile，短信验证每台机器只需一次。
- tkinter 图形界面与 PyInstaller 打包配置，同事双击即可使用，无需安装 Python。
- GitHub Actions 在 Windows 上构建并发布，公开仓库额度不限时长。
